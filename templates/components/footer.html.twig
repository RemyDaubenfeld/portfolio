<footer class="px-16 py-7 flex justify-center items-center gap-4 border-t border-border max-md:px-6">
    <span class="font-mono text-[0.65rem] text-text-mute tracking-[0.04em]">
        © {{ "now"|date("Y") }} {{ user.firstName }} {{ user.lastName }}
    </span>
    <span class="text-text-mute text-[0.65rem]">|</span>
    <a href="{{ path('app_mentions_legales') }}" class="font-mono text-[0.65rem] text-text-mute hover:text-gold transition-colors duration-200 no-underline">
        Mentions légales
    </a>
</footer>